<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "PaLevá" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" type="image/png" href="<%= image_url('android-chrome-512x512.png') %>">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <br>
      <div class="d-flex justify-content-center">
        <div class="card mb-3" style="max-width: 450px; padding: 10px;">
          <div class="row g-0">
            <div class="col-md-4">
              <%= image_tag 'android-chrome-512x512.png', class: 'img-fluid rounded-start', alt: 'paleva' %>
            </div>
            <div class="col-md-8">
              <div class="card-body">
                <% if user_owner_signed_in? %>
                  <div class="d-grid gap-2 d-md-flex justify-content-md-center">
                    <div class="d-grid gap-2 col-8 mx-auto"><%= link_to 'PaLevá', root_path, class: 'btn btn-warning btn-lg' %></div>
                    <%= button_to 'Sair', destroy_user_owner_session_path, method: :delete, class: 'btn btn-outline-dark btn-lg' %>
                  </div>
                  <br>
                  <p class="card-text">
                    <%= current_user_owner.name %> <%= current_user_owner.last_name %> <br>
                    <small class="text-body-secondary"><%= current_user_owner.email %></small>
                  </p>
                <% else %>
                  <div class="d-grid gap-2 col-10 mx-auto">
                    <%= link_to 'PaLevá', root_path, class: 'btn btn-warning btn-lg' %>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
        <% if user_owner_signed_in? && !current_user_owner.establishment.nil? %>
          <%= form_with url: search_establishments_path, method: :get, local: true do %>
            <div class="d-flex justify-content-center">
              <div class="row g-3 align-items-center">
                <div class="col-auto"><b><%= label_tag :query, 'Buscar Item', class: 'btn btn-outline-light' %></b></div>
                <div class="col-auto"><%= text_field_tag :query, params[:query], class: 'form-control' %></div>
                <div class="col-auto"><%= submit_tag 'Buscar', class: 'btn btn-warning' %></div>
              </div>
            </div>
          <% end %>
        <% end %>
    </header>
    <main>
      <br>
      <div class="d-flex justify-content-center">
        <div class="alert alert-warning" style="width: 28rem;">
          <%= alert %>
          <%= notice %>
        </div>
      </div>
      <section>
        <%= yield %>
      </section>
      <br>
    </main>
    <footer>
      <p>&copy;<a href="https://www.linkedin.com/in/marcos-grocha/" target="_blank"> marcos-grocha</a></p>
    </footer>
    <br>
  </body>
</html>
